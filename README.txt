La cartella contiene le classi che sono state realizzate per astrarre il sistema, correttamente compilate per essere importate.

Sono presenti inoltre due Jupyter Notebook: "SC3 - Soluzione finale" e "Confronto soluzioni alternative".
I due notebook presentano degli esempi di esecuzione con ciascun modello descritto nel report e sono presenti gli output riportati da ciascuno di essi.
Il primo notebook è quello che contiene il sistema migliore, proposto come soluzione per il problema di ricerca.

La cartella "Output" contiene invece dei file .csv, uno per ogni modello proposto.
Queste tabelle csv riportano le informazioni relative alla finestra in cui un cestino è stato svuotato, da quale veicolo, in che ordine sono stati svuotati e l'ora di visita prevista.